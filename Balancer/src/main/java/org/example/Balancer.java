package org.example;

public class Balancer {

    public static boolean isBalance(String input) {
        if (input.isEmpty())
            return false;

        for (int i = 0; i < input.length() - 1; i++) {

        }

        return false;
    }
}
